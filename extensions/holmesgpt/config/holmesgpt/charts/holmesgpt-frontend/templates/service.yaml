apiVersion: v1
kind: Service
metadata:
  name: {{ include "holmesgpt-frontend.fullname" . }}
  labels:
    {{- include "holmesgpt-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "holmesgpt-frontend.selectorLabels" . | nindent 4 }}
